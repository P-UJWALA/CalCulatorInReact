# Calculator Application
A simple React calculator application that uses the useState hook to manage input and results dynamically. It supports basic arithmetic operations and updates the UI instantly without reloading the page.
